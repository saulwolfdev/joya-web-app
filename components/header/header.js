import Navbar from '../navbar/navbar'

const Header = () => {
    return (
        <header className="fixed-top-scroll fixed-top">
            <Navbar></Navbar>
	    </header>
    );
}
 
export default Header;