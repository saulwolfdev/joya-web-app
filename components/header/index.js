import Navbar from '../NavBar'

const Header = () => {
    return (
        <header className="fixed-top-scroll fixed-top">
            <Navbar></Navbar>
	    </header>
    );
}
 
export default Header;